<?xml version="1.0" encoding="utf-8"?>
<style xmlns="http://purl.org/net/xbiblio/csl" version="1.0">
  <info>
    <title>Custom PNAS Style</title>
    <id>https://github.com/yourusername/custom_pnas.csl</id>
    <author>
      <name>Your Name</name>
    </author>
    <category citation-format="numeric"/>
    <summary>Custom CSL mimicking PNAS/Nature style with numbered references.</summary>
  </info>

  <!-- Author macro: Lastname, F. & Lastname, F. -->
  <macro name="author">
    <names variable="author" delimiter=", " and="symbol" initialize-with=". ">
      <name name-as-sort-order="all"/>
    </names>
  </macro>

  <!-- Title macro -->
  <macro name="title">
    <text variable="title"/>
  </macro>

  <!-- Journal macro: full name, italic -->
  <macro name="journal">
    <text variable="container-title" font-style="italic"/>
  </macro>

  <!-- Volume macro -->
  <macro name="volume">
    <text variable="volume"/>
  </macro>

  <!-- Issue macro -->
  <macro name="issue">
    <text variable="issue" prefix="(" suffix=")"/>
  </macro>

  <!-- Pages macro -->
  <macro name="pages">
    <text variable="page" prefix=", "/>
  </macro>

  <!-- Year macro -->
  <macro name="year">
    <text variable="issued" form="year" prefix="("/>
    <text value=")"/>
  </macro>

  <!-- Citation style: numeric in brackets -->
  <citation>
    <layout prefix="[" suffix="]">
      <text variable="citation-number"/>
    </layout>
  </citation>

  <!-- Bibliography layout -->
  <bibliography>
    <layout delimiter=". ">
      <text macro="author"/>
      <text macro="title" suffix="."/>
      <text macro="journal" suffix=" "/>
      <text macro="volume"/>
      <text macro="issue"/>
      <text macro="pages"/>
      <text macro="year"/>
    </layout>
  </bibliography>
</style>
